<h3>Informations sur la définition:</h3>
 <ul tal:condition="actions" tal:repeat="a actions" class="nav nav-pills nav-stacked">
   <li><a class="btn btn-primary btn-sm" style="max-width: 300px;" data-toggle="modal" href="#" data-target="#${a['action'].node_id}">
          <span class="glyphicon glyphicon-eye-open"></span> ${a['action'].title}
        </a>
    </li>
  <!-- Modal -->
  <div class="modal fade" id="${a['action'].node_id}" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
   <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
        <h4 class="modal-title" id="myModalLabel"> ${a['action'].title}</h4>
      </div>
      <div class="modal-body">
        ${structure:a['body']}

      </div>
    </div>
   </div>
 </div>
</ul>
<p><a href="${defurl}" class="btn">Voir plus de détails »</a></p>

<h3>Le modèle du processus:</h3>

<div>
Le graph du processus avec les actions en cours
</div>
